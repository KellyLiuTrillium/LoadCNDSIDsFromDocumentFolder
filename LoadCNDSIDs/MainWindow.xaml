<Window x:Class="LoadCNDSIDs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Button Name="selectFile" Click="selectFile_Click" Content="Select the file" Width="100" HorizontalAlignment="Left" Margin="5"/>

        <Button Name="LoadFile" Click="LoadFile_Click" Content="Load..." Grid.Row="1" Margin="5"/>
    </Grid>
</Window>
